spring:
  application:
    name: vet-clinic-system
  profiles:
    active: dev # Default profile (change to 'prod' to switch, or use CLI args)

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
  endpoint:
    health:
      show-details: always